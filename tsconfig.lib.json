{
    "extends": "./tsconfig.json",  // This line imports all configurations from the base tsconfig.json
    "compilerOptions": {
        // "baseUrl": "./src",
        // "declaration": true,        // Generates corresponding '.d.ts' file(s)
        // "outDir": "./lib",          // Redirect output structure to the lib folder
        // "noEmitOnError": true,      // Avoid emitting files if any error is found
        // "noImplicitAny": true       // Strengthen type-checking by disallowing implicit 'any' types
    },
    "include": [
        "**/*"              // Specific include path for library sources
    ],
    "exclude": [
        "node_modules",         // Exclude the node_modules directory
        "../../node_modules",
        "**/*.test.ts",
        "**/*.spec.ts",
        "**/*.stories.ts",
        "**/*.stories.tsx",
        "vite-env.d.ts",
        "main.tsx",
        "App.tsx",
        "App.css"
    ]
}